<nav>
  <% if user_signed_in? %>
    <%= button_to "logout", destroy_user_session_path, method: 'delete' %>
  <% else %>
    <%= button_to "sign in", new_user_session_path %>
    <%= button_to "log in", new_user_registration_path %>
  <% end %>
</nav>
