<div class="container">

<h3 class="regular-text">Welcome to Yumoo, <%= @user.username.capitalize %>!</h3>
<h4 class="regular-text">Your current mood is: <%= @user.moods.last.name if @user.moods.last %></h4>

<h3 class="regular-text">It's <%= Time.now.strftime("%A")%>
<% now = Time.now
today = Date.today.to_time
morning = today.beginning_of_day
noon = today.noon
evening = today.change( hour: 17 )
night = today.change( hour: 20 )
tomorrow = today.tomorrow
if (morning..noon).cover? now %>
morning 
<% elsif (noon..evening).cover? now  %>
afternoon
<% elsif (evening..night).cover? now  %>
evening
<% elsif (night..tomorrow).cover? now  %>
night</h3>  
<% end%>
<h3 class="regular-text">What do you feel like?</h3>

<!-- MOODS -->
<div class="container-moods">

<div class="row">
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[0].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[0].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[1].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[1].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[2].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[2].id } }, :class => "btn" %>
        </button>
    </div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[3].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[3].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[4].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[4].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[5].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[5].id } }, :class => "btn" %>
        </button>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[6].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[6].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[6].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[6].id } }, :class => "btn" %>
      </button>
    </div>
    <div class="col-sm-4">
      <button type="button" class="btn btn-primary btn-lg outline"><%= button_to @moods[7].name,{ controller: 'profile', action: 'recommend',
        method: 'post', params: { mood_id: @moods[7].id } }, :class => "btn" %>
        </button>
    </div>
  </div>

  </div>



<!-- FOODS RECOMMENDATIONS -->
<% if @recommendations %>
  <% @recommendations.each do |r| %>
    <%= r.food.name %>
  <% end %>
<% else%>
  <h1>Select your mood to get food recommmendation</h1>
  <h2>or hit the 'SURPRISE ME' button</h2>
<% end %>

</div>
